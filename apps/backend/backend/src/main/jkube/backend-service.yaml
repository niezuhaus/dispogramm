apiVersion: v1
kind: Service
metadata:
  namespace: ${jkube.namespace}
  name: backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8080
      nodePort: 31000